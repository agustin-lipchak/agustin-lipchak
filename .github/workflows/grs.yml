name: Update README cards
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=agustin-lipchak&show_icons=true&count_private=true&theme=dark&hide_border=true
          path: profile/stats.svg
          token: ${{ secrets.MYSITE_PAT }}

      - name: Commit and push if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: update automated GitHub stats card with dark theme and private stats"
          file_pattern: "profile/*.svg"
